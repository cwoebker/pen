language: python
python:
  - "2.7"
  - "pypy"

before_install:
  - pip install pipenv
  - pipenv install
install:
  - python setup.py install

before_script:
  - pip install nose

script:
  - nosetests
